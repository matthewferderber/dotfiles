# Before next commit / merge into main:

- Add oh-my-zsh as a dependency during bootstrap.
- Decide if `brew` should also be a dependency at setup time.
- Stop oh-my-zsh from overwriting the `.zshrc` in root
